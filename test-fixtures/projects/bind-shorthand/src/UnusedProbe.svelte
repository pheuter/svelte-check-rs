<script lang="ts">
  // Deliberately unused — confirms `noUnusedLocals` is active in this
  // fixture. Sanity check for the integration tests in
  // crates/svelte-check-rs/tests/integration_bind_shorthand.rs.
  let unusedProbe = 1;
</script>
